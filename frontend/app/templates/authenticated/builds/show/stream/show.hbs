{{#ui-header level=4}}{{model.name}}{{/ui-header}}
<div class="flex mb-3">
  {{#each model.boxes as |box index|}}
    {{box-tab box=box stream=model build=model.build index=index}}
  {{/each}}
</div>

<div class="w-full">
  {{outlet}}
</div>
