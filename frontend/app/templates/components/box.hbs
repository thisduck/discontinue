<div class="ui accordion fluid segments">
  {{#each @box.commands as |command index|}}
    <Command @command={{command}} @index={{index}} @toggles={{toggles}} @onToggle={{action 'onToggle'}} />
  {{/each}}
</div>
